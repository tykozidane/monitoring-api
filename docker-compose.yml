services:
  monitoring-dashboard:
    build: .
    container_name: api-monitoring
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "4000:4000"
    restart: always
    env_file:
      - .env
